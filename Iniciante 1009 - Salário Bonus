#include <stdio.h>
 
int main() {
 
    char JOAO;
    double SALARIO, VENDAS, TOTAL;
    
    scanf("%s", &JOAO);
    scanf("%lf", &SALARIO);
    scanf("%lf", &VENDAS);
    TOTAL = SALARIO+(0.15*VENDAS);
    
    printf("TOTAL = R$ %.2lf\n", TOTAL);
 
    return 0;
}
